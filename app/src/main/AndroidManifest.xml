<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ovalle.to_do">

    <application
        android:allowBackup="true"
        android:icon="@drawable/postit"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity android:name=".verNotaCom"></activity>
        <activity android:name=".verNotasCompartidas" />
        <activity android:name=".verNotas" />
        <activity android:name=".verAmigos" />
        <activity android:name=".SolicitudesAmistad" />
        <activity android:name=".VerTareas" />
        <activity android:name=".Menu" />
        <activity android:name=".forgetPassword" />
        <activity android:name=".registerTask" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>